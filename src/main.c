#include "pll.h"
#include "helpers.h"
#include <stdio.h>

int main()
{
    // 1. Create an instance of your PLL state machine.
    
    // 2. Initialize it to a known state using PLL_Init().
    
    // 3. Set a target frequency using PLL_SetFrequency().
    
    // 4. Run the main simulation loop, calling PLL_Update() repeatedly.
    
    // 5. Inside the loop, periodically print the status.
    

    return 0;
}